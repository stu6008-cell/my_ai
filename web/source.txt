# 미니 AI 가 읽는 정보 파일
#
# 여기에 적으면 옆에 있는 index.html 이 열릴 때 자동으로 읽어서 학습한다.
# 고친 뒤에는 페이지의 '새로고침' 버튼만 누르면 바로 반영된다.
# '#' 로 시작하는 줄은 무시되니, 아래 예시의 '#' 를 지우고 고쳐 쓰면 된다.
#
# 자동으로 읽히게 하려면 로컬 서버로 열어야 한다 (브라우저 보안 정책):
#     python3 -m http.server 8000
#     http://localhost:8000/web/ 접속
# 그냥 더블클릭으로 열었다면 이 파일을 창에 끌어다 놓으면 된다.


# --- 키: 값 (사실) ---------------------------------------------
# 좋아하는 음식: 마라탕
# 사는 곳: 서울 마포구
# 회사 출근 시간: 오전 9시 30분

# --- Q / A (명시적 문답) ---------------------------------------
# Q: 우리 강아지 이름이 뭐야
# A: 콩이야, 올해 3살

# --- 한 줄 문답 ------------------------------------------------
# 주말에 뭐 할까 | 한강 가서 자전거 타자

# --- X는 Y이다 (사실 + 자동 문답) ------------------------------
# 파이썬은 배우기 쉬운 프로그래밍 언어이다

# --- 그 외 문장은 말투 학습용으로 쓰인다 -----------------------
# 오늘은 비가 와서 하루 종일 집에 있었다
